node_modules/
dist/
*.tsbuildinfo
.DS_Store
coverage/
.vitest/
*.log
/tmp/
precision-report.md
.servanda-test-vault/

# Serena's local memories. Tooling state, not project truth — the durable record lives in
# Svod (projects/servanda/) and in docs/. Drop this line to version them instead.
.serena/
